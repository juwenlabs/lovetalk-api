name: patch-v35-realuser
on:
  pull_request:
    branches: ["main"]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply v35 patch
        run: python .github/scripts/patch_v35_realuser.py
      - name: Syntax check
        run: node --check server.js
      - name: Commit patched server
        run: |
          git config user.name "somtok-qa-bot"
          git config user.email "actions@users.noreply.github.com"
          git add server.js
          git diff --cached --quiet && exit 0
          git commit -m "Apply Potentia v35 real-user consistency guards"
          git push origin HEAD:main
